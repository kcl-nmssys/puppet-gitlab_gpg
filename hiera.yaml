version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Hiera data"
    paths:
      - "common.yaml"
      - "os/%{facts.os.family}.yaml"
      - "os/%{facts.os.name}.yaml"
