version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Hiera data"
    paths:
      - "os/%{facts.os.name}.yaml"
      - "os/%{facts.os.family}.yaml"
      - "common.yaml"
